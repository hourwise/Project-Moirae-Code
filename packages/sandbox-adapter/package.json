{
  "name": "@moirae/sandbox-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Sandbox execution adapter — host, restricted process, container, microVM, remote sandbox modes with evidence capture",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@moirae/runtime-contracts": "*",
    "@moirae/network-broker": "*",
    "@moirae/policy-profiles": "*",
    "@moirae/secret-broker": "*"
  }
}
