{
  "name": "@moirae/horae-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client for Horae session API — start, send, cancel, workflow state, runtime composition",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@moirae/runtime-contracts": "*"
  }
}
