{
  "name": "@moirae/ananke-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client for the Ananke HTTP API — execute, approve, deny, audit query",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@moirae/runtime-contracts": "*"
  }
}
