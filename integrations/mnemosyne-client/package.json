{
  "name": "@moirae/mnemosyne-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client for Mnemosyne MCP tools — search, context packs, memory read/write, revalidation",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@moirae/runtime-contracts": "*"
  }
}
